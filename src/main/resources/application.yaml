spring:
  application:
    name: collabo

  datasource:
    url: jdbc:mysql://localhost:3306/likelion
    username: root
    password: ${DB_PASSWORD}
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    hibernate:
      ddl-auto: create

cors:
  allowed-origin: http://localhost:3000, http://localhost:5173, https://api.eunhasu03.cloud